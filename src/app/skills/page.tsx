"use client";

import NavigationTabs from "@/components/tab-navigation";
import styles from "@/styles/home.module.css"; // Utiliser l'alias @ pour référencer le dossier styles
import { useState } from "react";
import AlertDialog from "@/components/dialog";
import { useDialog } from "@/components/dialog/dialog_context";

export default function Page() {
  return <p>Skills page</p>;
}
